✅ 2. Make sure it's not hidden or filtered
If the file is there but not showing:

Click the Explorer filter icon (top of file list) and ensure "Show All Files" is selected.

Check your .vscode/settings.json to see if any files are excluded:

json
Copy
Edit
"files.exclude": {
  "**/README.md": true
}
